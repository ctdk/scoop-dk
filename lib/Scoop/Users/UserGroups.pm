package Scoop;
use strict;

# This is just some stuff to have collaborative editing of stories and 
# private user groups in Scoop. Who knows if it will actually see the light
# of day or not, but perhaps some might find it useful.

sub user_group_page {
	my $S = shift;
	my $ugroup = $S->cgi->param('ugroup');
	my $page = $S->cgi->param('page');

	}








1;
